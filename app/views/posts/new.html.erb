<h1>New Post</h1>

<!--
<%= render 'form', post: @post %>
-->
<%= form_for(@post) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :description %>

  <%= f.submit "Update Post" %>
<% end %>

<%= link_to 'Back', posts_path %>

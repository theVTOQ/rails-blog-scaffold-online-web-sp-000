<h1>Editing Post</h1>

<!--
<%= render 'form', post: @post %>
-->
<%= form_for(@post) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area_field :description %>

  <%= f.submit "Update Post" %>
<% end %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>
